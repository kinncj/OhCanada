# Delivery plan

State lives here, not in conversation history. Update the status column when a slice lands.

| # | Slice | Goal | Status |
|---|---|---|---|
| 0 | Foundation | Harness, repo skeleton, Makefile, CI + Pages deploying an empty portrait canvas, ADR-0001..0008 | Done |
| 1 | Vertical proof (see `slice-1.md`) | Level 4 — How Canadians Govern Themselves (Ottawa, skate). Boot → creator → level → officer → quest → questions → Study mode → save/reload | **Done** — 2026-09-13. The officer, the quest and the character creator all landed; the loop runs end to end on Pages. 2026-09-14: the creator offers six slots — `presentation` opened, 22 options, 1 440 appearances — with skin swatches drawn from `palette.json` and no dead Settings button; a save written before a slot existed takes that slot's fallback silently, and only an option this build no longer offers is reported as a repair. The figure was re-proportioned, faces became `face-{expression}-{presentation}`, and the skateboard stance was redrawn. The level-select map marks where the player is and draws the travelled route. Later on 2026-09-14 the player, the officer and the guide were **redrawn** after the PO called the look goofy (`assets/style/art-bible.md` §7.7):
- rounder heads with the face on their leading half
- every joint closed by caps centred on its pivot
- a puffed coat with a waist
- chunkier mitts and boots
- idle arms re-solved

The rig is otherwise unchanged: the same parts, pivots, slots and mode poses. The shared 2× atlas went from 11.09 to 10.27 MiB on every level. A `skin-1` lightening is **proposed, not applied**, pending the PO's cultural sign-off (art-bible §8.1). **Art verification is owed:** `docs/art-verification.json` holds a verdict for the officer only, and its sources changed again with the redraw, so it is stale. The player, the guide and the four mounted subjects have no verdict. `make verify-art` also reports the Dufferin Terrace and Halifax quayside verdicts stale, because their level art moved earlier. A fresh blind `make verify-art` run is needed. Later on 2026-09-14 the character creator **shows the character it describes**, answering an audit P1 ("Your character" was a sentence with no picture): the level's sprite puppet painted into a 2D canvas beside the words, with no second Phaser game (ADR-0040). It redraws on every choice, holds still under reduced motion (the setting or the device), is sticky at 100 % text and stacked above it, falls back to words only when the atlas fails, and is released when the creator closes. It costs the shared character atlas page on open (290 KB at 2x, 147 KB at 1x, plus 37 KB of frame data), a cache hit for the level and nothing in the initial payload; `character-preview-art` reports `data-frames`, which `tests/e2e/creator-preview.spec.ts` reads |
| 2 | Scale proof | Level 3 — Canada's History (Québec City, toboggan), added by JSON + assets only | **Done** |
| 3 | Level 1 — Rights and Responsibilities (Halifax, walk) | Level + quest + verified questions | **Done** |
| 4 | Level 2 — Who We Are (Peggy's Cove, walk) | Level + quest + verified questions | **Done** — the place is a Nova Scotia fishing village, not a territory, and the locomotion is walk, not canoe. `docs/content-review.md` §1 blocks a level whose *subject* is a nation's territory; the quest is offered by the lighthouse itself under ADR-0029, because this level draws no figure at any scale |
| 5 | Level 5 — Federal Elections (Toronto, bike) | Level + quest + verified questions | **Done** — three points of interest since 2026-09-13: a streetcar teaching which services local governments run (p. 66), the CN Tower, and Nathan Phillips Square teaching where most Canadians live (p. 98), drawn from licence-checked photographs. All three are quest stops since 2026-09-14, so the level asks 8 questions where it asked 5. No licence-clean photograph of a polling station interior exists, so the level's own subject still has no object to stand on. Since 2026-09-14 the rider draws whole where the drive stops on the guide: every placed character drew its parts at depth `500 + z` and the player at `501 + z`, so the two puppets interleaved wherever a drive stopped on a character — on all eight levels that place one, in every mode. `depth-plan.ts` gives each character its own slot, and `data-parts-interleaved` reads 0 on all ten levels |
| 6 | Level 6 — The Justice System (Winnipeg, walk) | Level + quest + verified questions | **Done** |
| 7 | Level 7 — Modern Canada (Prairie rail, train) | Level + quest + verified questions | **Done** — since 2026-09-14 the player rides the train. A player reported the figure "walks by itself on a track"; the level now declares a `ride` (ADR-0031), the rear of VIA Rail's *Canadian* with the player seated in its dome, running on a nearer line so the landmarks and the guide stand beyond it, and `train/*` seated poses in the rig. The level's texture budget went from 36 to 40 MiB because the car costs 3.2 MiB and the perf lane counts unpriced GPU bytes against the same number. Since 2026-09-14 the seated passenger's `train/interact` reach holds on screen while an engagement's card is open (`engagement-pose.ts`). Since 2026-09-14 a level declares its `weather`, required and with no default, so snow no longer falls on this harvest level: **`snow`** on Ottawa and Québec City; **`none`** on Halifax, Peggy's Cove, Toronto, Winnipeg, the Prairies, the Alberta foothills, Vancouver and the North, each as its `assets/style/<id>-level.md` states, held by `a-level-snows-only-where-its-art-sheet-says.test.ts`. A `none` level emits 0 particles at every tier; the particle checks still run on Ottawa. The same day the dead `airborneInput`/`brakeTrigger` bindings left the schema, the port and all ten levels, so no level logs the binding error, and `a-level-binds-only-what-the-rig-declares.test.ts` holds what remains to `rig.json`. A brake pose is still `OQ-RIG-4` |
| 8 | Level 8 — Canada's Economy (Alberta foothills, horse) | Level + quest + verified questions | **Done** — a pump jack joined the ranch gate, barn and cattle on 2026-09-14, teaching that Alberta produces more oil and gas than anywhere else in Canada (p. 101), and the quest now stops at all four, asking 9 questions where it asked 7. Since 2026-09-14 the player **rides a saddled bay ranch horse**. A play-through audit found the HUD saying Horse while the player walked the level on foot. The level now declares a ride (ADR-0031): one `behind` layer drawn from a CC0 quarter-horse side view, a CC BY 2.0 western-saddle photograph and Muybridge's plate 574, on a world-fixed trail strip 340 px below the walking line, so the gate, barn, pump jack and cattle stay visible at each stop. Eight seated `horse/*` poses are in both rig files, and `a-rider-stays-on-the-ride.test.ts` holds both feet to the ride. **Since 2026-09-14 the horse walks** (ADR-0035, branch `ride-animation`, closing `OQ-RIG-2`): a ride layer may declare a `cycle` of frames advanced by distance travelled, not time, so the gait keeps pace with the trail and stops when the player stops. The horse draws four walk frames off Muybridge's plate 574 (three hooves down, one lifted, in the lateral order), one per 55 px, which is a planted hoof's step and a quarter of the 220 px bob; a standing frame at rest; and one stride, never advancing, under reduced motion. The saddle is the SVG group `seat`, byte-identical in all five files, and `level-art-is-placed-where-it-is-drawn.test.ts` fails if it moves or names another anchor. Texture 30.16 of 36 MiB (84 %, +4.08), payload 0.67 of 8 MiB; with the jacket's larger shared atlas (A2), on branch `polish-train`, 31.18 of 36 MiB (87 %) and 0.73 MiB. No run gait: the walk plays faster as the ground does. The Prairies train is not animated: the Park car's wheels are drawn, as the reference's trucks carry them, as plain dark discs with a centred hub, so turning them changes no pixel, and any other frame change is a second 1 420 × 590 texture at 3.20 MiB (30.85 to 34.05 of 40 MiB) for a detail at the bottom edge of the screen. Open: the first-visit hint panel covers the horse's legs at the first two stops. The rig parts that drew out of order in the first renders draw correctly since the per-character depth fix. `make verify-art` has no verdict for `ranch-horse` yet |
| 9 | Level 9 — Canadian Symbols (Vancouver, skateboard) | Level + quest + verified questions | **Done** |
| 10 | Level 10 — Canada's Regions (the North, walk) | Level + quest + verified questions | **Done** — set at Whitehorse on the Yukon River. Recorded in `assets/style/the-north-level.md` §11 as a **scope decision rather than a solution**: a level about Canada's regions set in the North either depicts the peoples of Inuit Nunangat or removes them from a level about where they live, and this project has no reviewer who may say which. Locomotion is walk, not dogsled |
| C1..C10 | Content slices | ≥ 30 verified questions per subject | **Done** — 481 verified. economy 49, elections 36, government 38, history 96, justice 39, modern-canada 40, regions 58, rights 37, symbols 42, who-we-are 46. Levels now ask 5–9 questions each rather than 3, so the two thinnest banks were funded: 8 justice and 16 economy questions added 2026-09-13; 24 granted after one round of rewrites, 1 left rejected. **Justice stops below 40 by decision, not shortfall** — `jus-33` is unrecoverable (true on the page, false in the world, and its recoverable form belongs to government) and `jus-37` survives only as its "Supreme Court" half, because the guide's "Court of Queen's Bench" is now King's Bench — every sentence on pages 75–77 is already a question and almost all other justice material belongs to another subject's remit (habeas corpus and jury duty to rights, criminal law as federal to government). Reaching 45 would mean moving propositions between subjects under ADR-0028, not worth it for a handful of questions; 39 clears the floor of 30. Economy can reach 45 with material left (pp. 96–98, 102–103) |
| L1 | Learning loop (ADR-0036) | Questions from the level's subject at the place that taught them; a stamp only for the task; a card that counts something | **Done in code, content owed** — 2026-09-14, branch `place-questions`, after a play-through of the live build. **Questions:** `SceneLevel` carries `subject`, and every in-level question comes from it. A landmark on an `answer` step asks all that step has left, and the card counts the step ("Question 2 of 2"). A question resting on the landmark's own `source.quote` is asked first. Accepting a `talk` → `answer` quest (Peggy's Cove, the North) asks at the giver. **Stamps:** reaching the end of a level whose task is not done earns nothing and opens nothing; the card there says what is left and gives the level back (`TN-DONE-09`), and `OQ-PASSPORT-8` is closed in the passport's favour. **Card:** no counter over one question, the tag is words, and "Close" goes once the last answer is given. Five copy rows are `COPY_GAPS` awaiting ratification. **Pools:** since 2026-09-14 (branch `quest-question-pools`) all 32 `answer` steps name a `questionPool`: the verified questions in the level's subject that are about what the step's own prompt says. Pools within one quest do not overlap. `an-answer-steps-pool-can-fill-its-count.test.ts` requires a pool on every answer step. Every pooled id must exist, be askable (the rule `isShippable` states) and sit in the level's subject, and each pool must hold at least the step's count. Three steps changed: Peggy's Cove's light asks about "English and French in Canada", not "the two official languages"; Peggy's Cove's village house asks 1 question about becoming Canadian, not 2; and Québec City's château asks about "Canada's name and the first explorers", not "how this country got its name". Toronto's "voting where you live" kept its wording, with five questions. Six pools hold exactly their count, so a replay asks the same questions: Halifax's voting and obeying the law, the Prairies' trade after the Depression, Québec City's first elected assemblies, the North's land and rivers, and Winnipeg's legal help. **Owed, content:** 24 of the 35 landmarks tell a sentence that no question in their level's subject rests on. See *L1 gap list* below. **Owed, tests:** no a11y spec scans the completion card in either state, nor the one-question card with no counter (the harness draws only the finished card and a three-question card). `markLevelComplete` now runs when the finished card is drawn, on both routes. Unit, e2e and a11y suites were not run locally; CI runs them |
| F1 | Exam mode | Official format, optional timer, results by subject | **Done** — 2026-09-09 |
| F2 | Accessibility pass | Full audit: keyboard, switch, screen reader, scaling, contrast | **Substantially done** — `tests/a11y/` runs axe-core on every DOM screen, including "About this place" and the level-select map, single-switch and 200% text covered. Not signed off as an audit. Since 2026-09-14 **every drive stops at each thing it can engage, once per visit** (ADR-0032, `TN-REACH-09`): a held drive comes to rest at each landmark and character and goes on with a fresh press, engaging, or steering back; auto-move and the train stop at the same places, finished subjects included. Engaging by the interact prompt now releases the stop — before, an auto-move or one-switch player who engaged by the prompt was left held at the landmark. Browser walks let go and press again at each stop through `tests/e2e/walk.ts`; `tests/e2e/held-move-stops.spec.ts` is new. Later on 2026-09-14, after a live-site audit (ADR-0037, `TN-REACH-10`, `TN-REACH-11`): **letting go inside reach stops the player there** — a skater who let go as the prompt appeared used to glide 2 000 px past it — while a glide let go outside reach still coasts past everything; a subject the stop holds **stays on offer** past the edge of reach; and **a stop rests beside a character, not inside them**, measured from the rig's frame windows plus a ride's new required `footprint`. No tuning and no placement changed. `tests/unit/contracts/a-stop-rests-beside-a-character.test.ts` and `tests/e2e/release-in-reach.spec.ts` are new. The Prairies train now stops 274 px past the guide, clear of its dome. Since 2026-09-14 the live-site audit's findings are fixed (ADR-0039). **"Less movement" in Settings now reaches the renderer** at boot and on every change: it used to restyle only the DOM, so particles and parallax easing stayed on and only the OS preference worked (`tests/e2e/level-ottawa.spec.ts` toggles it in-game and reloads). The HUD draws the prompt, Settings and Menu first, and the chrome around the text holds still at large text, so the action is inside the strip at 200 % in EN and FR. Map cards and passport rows keep their width and shape at 200 %; option lists lost a stray 1.25rem indent; a radio dot or tick stays beside its words. Landmarks are named in their prompt (27 proposed rows), a giver with an unfinished quest is no longer "Done", the hint fits a person, "Finish The Prairies first." is written out per level, and readiness counts are gone from a complete build. The 38 `COPY_GAPS` rows this audit added are pending ratification in `TN-REACH` and `TN-MAP`. Not yet done: single-switch mode does not drive the player on its own (`TN-LEVEL-07`) |
| F3 | PWA and offline | Workbox per level, installable, offline after first load | **In progress** — the infra half landed 2026-09-14 (ADR-0034). `dist/sw.js` is a Workbox worker built by `scripts/lib/pwa.mjs`: navigations and `manifest.json` from the network first with no timeout, the shell and every code chunk precached (30 files, 3.46 MB, held under the 8 MiB initial-payload ceiling by `deploy-check`), each level's art cached whole the first time it is played, and a new build taking over as soon as it installs. `dist/manifest.webmanifest` and three icons drawn in `assets/src/icons/` make it installable; headless Chromium against `vite preview` reported no installability errors. `featureFlags.serviceWorker` is the kill switch: off ships the tombstone (runbook §3c). The browser suites run with `serviceWorkers: 'block'`; `tests/e2e/offline.spec.ts` plays a level offline and has not yet run in CI. The in-game "A new version is ready" notice landed 2026-09-14 and discharged ADR-0034's `ui-a11y` obligation: `app/ui/update-notice.ts`, triggered from `app/bootstrap/update-notice.ts` when a newer worker takes over a page that was controlled when it loaded, deferred while a question card, the exam or a quest dialogue is open, never on a first visit; registration stays build-time. Its unit, axe (`tests/a11y/update-notice.spec.ts`) and end-to-end (`tests/e2e/update-notice.spec.ts`, a real second worker revision) specs are written and have not yet run in CI. **Not done:** the re-drill of rollback across the worker (runbook §6) |
| F4 | Performance pass | Every target device against the budgets | **In progress.** Since 2026-09-13 CI measures what a GPU-less runner can, and blocks on it: overdraw per visited tier and GPU texture memory counted at the WebGL API (calibrated in the same run), plus payload (`tests/perf/README.md`). Frame time and per-character cost are device-only (`make test-perf-device`) and have **no recorded pass**. Since 2026-09-14 the tier tracker settles instead of cycling low/medium on SwiftShader, and `?e2e=1&tier=` pins a tier for the perf suite (ADR-0011 amendment). Particles are checked against 400/1500 at a pinned `high`, on Ottawa, one of the two levels whose `weather` is `snow` (a `none` level emits 0, which that check reads as not measured); overdraw is also checked pinned at `low`, `medium` and `high`. CI measured 2.21x at `low` and 2.66x at `medium`; `high` has been gated since 2026-09-14 on an estimate of about 3.3x of 4x, and its first CI reading replaces that estimate. Since 2026-09-14 atlas pages come from `scripts/lib/atlas-pack.mjs` (ADR-0033). **Smaller:** `shared@2x` is 1278×1805, 8.80 MiB, down from 10.27 MiB, so every level is 1.47 MiB lighter at 2× and 2.96 MiB lighter at 1×; Halifax is at 77.5 % of budget and Toronto at 77.3 %. **Stable:** under ±2 px of frame drift the page moves 0.13 MiB, where it used to move 5.97 MiB. **No hairlines:** the faint rectangles round character parts were a ring the old packer painted round every frame; they are gone, and `make assets` fails if they return. **Still open:** the loading half of `OQ-RIG-1` |
| F5 | Docs completion | ADRs, architecture, art bible, content review, runbook, credits, contributing | **In progress** — 39 ADRs, `docs/guidelines/` for outside contributors, art sheets per level. a runbook covering the perf lanes; no contributing guide |
| A1 | Level art polish (live-site audit, 2026-09-14) | Sky seams, empty ground band, Peggy's Cove clouds | **In progress** — branch `level-art-polish`. **Seams, fixed at three sources.** (1) Every tile top in the sky drew a one-pixel line in the colour of the tile's foot: a TileSprite wraps its texture coordinate with `mod(texCoord, 1.0)` and an antialiased edge pixel is shaded just outside the tile, so it wraps to the last rows. `scripts/assets.mjs` now appends 4 transparent rows under every layer image placed below world 0 and reads them back from the WebP (33 files; +0.09 MiB Halifax, +0.12 Ottawa; no tier keeps a different layer set). (2) On a phone taller than 9:16 `Scale.FIT` letterboxes 76 CSS px above and below the canvas, and `index.html` painted the sky-to-ground ramp over the whole page there; the ramp now runs over the canvas box, so the band above it is flat `theme.sky`. (3) `ottawa.json` said `sky-base` over a sky layer that opens on `sky-shade`; it now takes `palette.json`'s `sky-shade`. Letterbox step at 390×844 measured 8–48 per channel before, 5–7 after. **The 5–7 left is not art:** `GameRenderer.cssVariables()` hands the page the time-of-day-tinted sky while layer textures are never tinted, so the step grows toward night (the sky is pulled up to 45 % toward `#101a33`); closing it is an `app/` change. **Peggy's Cove clouds** are fifteen scattered rolls, no longer five rows. **The empty ground band is NOT fixed, and cannot be from art or camera.** The ground polygon is painted opaque at depth 400 over every parallax layer, so no `layers[]` entry can dress the ground below y 1280 (art bible, "Nothing you draw below the ground line"). `camera.offset.y` does nothing on any level: `size.y` equals the design height, so the scroll clamps to 0. `zoom` can only shrink the band by putting the player below two thirds of the canvas (ADR-0002) and cropping landmark heroes. It needs an engine change (a ground-dressing tile drawn above the fill, or anchoring the canvas low on tall phones under a new ADR), then per-level art |
| A2 | Seasonal outfit (live-site audit, 2026-09-14) | The player dressed for the level's season, not in a parka everywhere | **Done, verification owed** — art on branch `seasonal-outfit`, wiring on `polish-train`. A live-site audit photographed the player in a parka, scarf and mitts on Winnipeg and on every summer level. The rig gains `costume: jacket`: a light cobalt jacket worn open over a white T-shirt, jeans rolled once, red canvas high-tops and bare hands. It is drawn for every skin tone, hair shape, hair colour, head covering, feature and presentation, and posed in every mode: walk, bike, skateboard, the train seat, the horse, skates and toboggan. The bare hands are two new parts on `bare-hand-{costume}-{skin}`, drawn in all six ramps for the jacket only, and each carries its hand's transform in every key (`rig-is-coherent.test.ts`). Every level art sheet names the player's costume beside its weather: `parka` on Ottawa and Québec City, `jacket` on the other eight. Every jacket frame window sits inside the parka's, so no stand-off span changes. The officer and the guide are unchanged. `shared@2x` went from 8.80 to 9.82 MiB on every level. On `polish-train`, with the walking Alberta horse, all ten pass `make assets`: the tightest is Alberta at 31.18 of 36 MiB (87 %), then Halifax at 81 % and Toronto, Winnipeg and the Prairies at 80 %. **Wired** (branch `polish-train`): a level declares a required `playerCostume`, with no default, in `level.schema.json`, `LevelDocument` and `level-document.ts`, and `level-scene.ts` puts it on the player over the creator's choices: `parka` on Ottawa and Québec City, `jacket` on the other eight. `a-level-dresses-the-player-for-its-season.test.ts` holds each level to its art sheet and each offered costume to the rig. Stand-off is measured from the player artboard's own costume, so no stop moved. Headless renders at 390 × 844 show Toronto and the Alberta foothills in the jacket and Ottawa in the parka. **Open:** the creator picture (ADR-0040) still draws the artboard's parka, which eight levels no longer draw, and `character-preview.test.ts` pins `character-torso-parka`; stand-off measures the parka only, and the jacket's extent equals it in every mode today, but nothing fails if a jacket frame is later drawn wider. The toque stays, because it is the player's choice. **Owed, verification:** no blind `make verify-art` verdict exists for the player in either costume, and none can for the jacket yet: `scripts/lib/art-handoff.mjs` pins the player's hand-off figure to `parka` and `assets/refs/references.json` names no jacket subject |
| A3 | Beaver guide redesign (PO: "the beaver looks like a crackhead", 2026-09-14) | A warm, soft, trustworthy guide on the same rig and proportion canon | **Done, verification owed.** Branch `beaver-guide`, rebased on `main` (5e0caa2).<br><br>**Diagnosis**, measured before drawing (`assets/style/guide.md` §0). The guide plays the shared eyes, so the stare came from what surrounds them: `leather-base` fur as dark as `skin-5`, with 19.4 L\* of brow contrast against the player's 42.4. Also: a column body as wide as its head, on 131.5 px of human leg; 22 × 21 px incisors hanging below the snout pad under a hidden smile; 13.7 % of the figure in dark non-ink fills.<br><br>**Design.** Three directions were sketched at 390 px (chestnut, plush mask, natural leather). Chestnut was chosen and iterated three times. The ten `*-beaver` parts are redrawn: a `wood` pelt, a cream bean muzzle carrying the shared smile with two 17 × 14 px teeth tucked under it, an ear on the skull's edge, a pear body hemmed below the knee with a cream front, tapered forelegs with no cuff, warm `leather` paws and feet, and a crosshatched paddle. No rig part, pivot, key, slot or face frame changed. No accessory: `references.json` `neverAdd` forbids one.<br><br>**Measured.** The pelt covers the shared head, crop and neck pixel for pixel. Rest width is 116 px (cap 116). The guide's stand-off span shrank from {-100, 62} to {-96, 61}, so every stop keeps 16 px of clearance and gains reach headroom: Québec City's toboggan 7.67 → 8.67 px, every walk heading left 11.6 → 15.6 px, the Prairies' train 17.4 → 21.4 px. `shared@2x` went from 1425×1806 (9.82 MiB) to 2043×1286 (10.02 MiB), +0.20 MiB on every level. All ten pass `make assets`; the tightest is Alberta at 31.38 of 36 MiB (87 %), then Halifax 27.67 of 34 and Toronto 27.57 of 34 (81 %).<br><br>**Owed.** `docs/art-verification.json` holds no `guide` verdict, and none was recorded here. The guide's `mustBeRight` in `references.json` now describes the redraw (small incisors, the tail and incisor mask regions, ear, muzzle, belly, feet) and needs a blind verifier run. |

## L1 gap list

These are the 24 landmarks where no question in the level's subject rests on the sentence the landmark tells (ADR-0036 §2.4). They ask a question from the subject, not about the place. This list is for a later **content-author** pass, then a separate **content-verifier** pass. Nothing here is authored or granted.

**Rules for using a row.**
- **A sentence another subject already grades cannot take a second question** (ADR-0028 §4, ADR-0030). For those landmarks, the author writes a question on the proposition below, and the blurb is re-pointed to that sentence.
- **Every quoted sentence was checked on 2026-09-14.** It is in `content/sources/discover-canada-2012-large-print.txt` on the page given, and no question in any subject rests on it. The check used `a-proposition-belongs-to-one-subject`'s normalisation, in both directions.
- **That check cannot see paraphrase.** Rows marked *close to* name a question the verifier must compare.
- ***Weak*** means the link to the place is thin. Prefer a better blurb over forcing it.

| Level (subject) | Landmark | Its sentence is graded by | Proposition a new question could test (p.) | Link, and caveats |
|---|---|---|---|---|
| Alberta foothills (economy) | `ranch-gate` | `reg-43` | "For centuries Canada's economy was based mainly on farming and on exporting natural resources such as fur, fish and timber, transported by roads, lakes, rivers and canals." (29) | A ranch is farming |
| Halifax (rights) | `town-clock` | `hist-24` | "One of the privileges of Canadian citizenship is the right to vote." (61) | The clock tells of the first elected assembly. Close to `rights-29` |
| Halifax (rights) | `pier-21` | `wwa-36` | "Such experiences do not justify bringing to Canada violent, extreme or hateful prejudices." (20) | Pier 21 is where newcomers landed. The sentence sits in *Who We Are*: settle its remit before authoring |
| Halifax (rights) | `harbour-tug` | `eco-36`, `reg-25` | "By helping to protect your community, you follow in the footsteps of Canadians before you who made sacrifices in the service of our country." (15) | *Defending Canada*. The blurb is about Halifax's defence role |
| Ottawa (government) | `rideau-locks` | `reg-05` | "Her Majesty is a symbol of Canadian sovereignty, a guardian of constitutional freedoms, and a reflection of our history." (57) | *Weak*: only that the Sovereign chose the capital. Check the symbols remit (`sym-02`, `sym-03`) |
| Ottawa (government) | `library-of-parliament` | `sym-17`, `sym-18` | "Second Reading – Members debate the bill's principle." (56) | The bill step is asked here. `gov-20-*` already grade first reading and committee stage |
| Ottawa (government) | `parliament-hill` | `sym-16`–`sym-19` | "Senate – The bill follows a similar process in the Senate." (56) | Both chambers sit in these buildings |
| Ottawa (government) | `warming-hut` | nobody, but its Rideau Canal caption (94) is regions | None found in government | Re-point the blurb instead. Closest existing: `gov-04` (defence and navigation are federal) |
| Peggy's Cove (who-we-are) | `granite-shore` | `reg-02` | "Canada has welcomed generations of newcomers to our shores to help us build a free, law-abiding and prosperous society." (5) | *Weak*: the shore |
| Peggy's Cove (who-we-are) | `peggys-point-light` | `eco-35`, `reg-24` | "Today, Acadian culture is flourishing and is a lively part of French-speaking Canada." (19) | Nova Scotia's identity. The Maritimes are the Acadian homeland |
| Peggy's Cove (who-we-are) | `fish-store` | `eco-20` | "Generations of pioneers and builders of British origins, as well as other groups, invested and endured hardship in laying the foundations of our country." (20) | Same theme as the blurb. Close to `wwa-35`, which quotes the next sentence and whose answer restates this one |
| Peggy's Cove (who-we-are) | `village-house` | nobody, but its sentence (94) is where Canadians live, which is regions | "Non-official languages are widely spoken in Canadian homes." (21) | *Weak*: a house is a home |
| Prairies (modern-canada) | `grain-bins` | `eco-29`, `eco-43` | None found | Closest existing: `mc-03` (1951: food, shelter and clothing) |
| Prairies (modern-canada) | `combine-harvester` | `eco-27`, `reg-37` | None found | Closest existing: `mc-02` (oil in Alberta, 1947) |
| Prairies (modern-canada) | `container-car` | `eco-01` | "Between 1945 and 1970, as Canada drew closer to the United States and other trading partners, the country enjoyed one of the strongest economies among industrialized nations." (45) | Trade. Would join the combine's trade pool (`mc-01`, `mc-08`), which holds exactly its count |
| Québec City (history) | `terrace-kiosk` | `sym-34`, `sym-35` | "The British paid for a costly Canadian defence system, including the Citadels at Halifax and Québec City, the naval drydock at Halifax and Fort Henry at Kingston—today popular historic sites." (30) | Names Québec City's Citadel |
| The North (regions) | `spruce-stand` | `eco-32`, `eco-33` | "The Northern territories have long cold winters and short cool summers." (103) | The territories. The sternwheeler's quote contains this sentence too, so a question on it would be asked first at both |
| Toronto (elections) | `streetcar` | `gov-59` | "It is important to find out the rules for voting in provincial, territorial and local elections so that you can exercise your right to vote." (66) | Local government. Close to `gov-60`, which sits in the same step's pool |
| Toronto (elections) | `cn-tower` | `eco-26` | "You can see the results on television or on the Elections Canada website (www.elections.ca)." (65) | *Weak*: the tower's broadcasting role is not in the guide, so the link cannot be told |
| Toronto (elections) | `nathan-phillips-square` | `reg-30` | "It is important for Canadians aged 18 or more to participate in their democracy by voting in federal, provincial or territorial and municipal elections." (56) | **Not usable as it stands**: `gov-21` already asks "How is voting described for Canadians aged 18 and over?" in government |
| Vancouver (symbols) | `marina` | `reg-46` | "Lieutenant Robert Hampton Gray, a navy pilot born in Trail, B.C., was killed while bombing and sinking a Japanese warship in August 1945, a few days before the end of the Second World War, and was the last Canadian to receive the V.C. to date." (87) | *Weak*: British Columbia and the sea only |
| Vancouver (symbols) | `bulk-carrier` | `eco-47` | None found | Closest existing: `sym-13` (*A Mari Usque Ad Mare*) |
| Winnipeg (justice) | `footbridge` | `reg-39` | None found | Closest existing: `jus-21` (municipal police). *Weak* |
| Winnipeg (justice) | `autumn-maple` | `sym-09`, `sym-10` | None found | Closest existing: `jus-07` (Lady Justice). *Weak* |

## Rules
- A level must be addable by JSON and assets alone. If a level needs engine changes, the schema is wrong —
  fix the schema first (that is what slice 2 proves).
- Content slices touch only `content/` and can run beside level work.
- Nothing after slice 1 starts until slice 1 is live on Pages with `verify-art` and `verify-content` green.
- A gate is not trusted until it has been seen to fail on a real violation. Slice 0's audit found three that
  measured nothing and two skipped tests that asserted `0 === 0`; assume the next one is in here too.
